# Regard — UX/feature feedback round (2026-08-29 testing)

Captured from a hands-on testing pass. Grouped by area; each item tagged with a rough
effort — **[S]** small (an afternoon), **[M]** medium (a day-ish, some backend), **[L]**
large (a real feature, multi-file/schema). Notes in _italics_ are my read on the item,
not part of the request. Proposed batching is at the bottom.

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done (link the commit).

---

## A. Notifications & job feedback

- `[x]` **[S] Colour the bell dot by contents.** Just-finished jobs → green, any errors →
  yellow, pending/running → cyan. Priority = attention(yellow) > pending(cyan) > finished(green).
  _Done (Batch 1, uncommitted): `NotificationsService.DotState` + `_nav.scss` neon-glow classes;
  all three colours + priority verified live/harness with Playwright._
- `[x]` **[M] Richer download notifications** — show download speed, ETA, total size on the
  live card. _yt-dlp progress output already carries speed/ETA/size; the Phase-A regex that
  feeds the pie can capture them. Live values live in `JobTrackerService.liveJobs`._
- `[x]` **[S] Stop showing "ETA ~1–2 min" on queued jobs.** These are never accurate for
  downloads. Show a queued/indeterminate state instead of a fake minute count. _Done (Batch 1,
  uncommitted): `DownloadVideoJob` queued text now reads "position N in the youtube.com download
  queue" / "waiting for a slot", no minutes. Verified live._
- `[x]` **[M] Cancel queued / retrying downloads too**, not only actively-running ones.
  _Cancel already exists for running jobs (`Video.DownloadSkipped`); extend to
  `Scheduled`/deferred + retry-waiting states._
- `[x]` **[S] Align "History" and "Clear all"** in the notification area header (vertical
  alignment is off). _Done (Batch 1, uncommitted): `_jobs.scss` header rows `baseline`→`center`.
  Verified live._

## B. Player / watch page

- `[x]` **[M] Likes/dislikes not visible or working** in the player — **this is a bug**, not
  a missing capability: we already use **Return YouTube Dislike**, so both counts should be
  available and shown. Triage where it breaks (fetch → store → API → render) and surface
  both like and dislike counts. _yt-dlp gives `like_count`; dislikes come from the RYD API._
- `[x]` **[S/M] Description formatting + typography.** Linkify URLs/@handles/#tags and make
  `mm:ss` timestamps seek the player. _Some creators do type markdown-ish text (e.g. `##`
  headings), so a **light** markdown-ish render (headings/bold/lists) may be worth it on top
  of linkifying — not a full parser. Improve spacing/line-length/font either way._
  **Typography done in Batch 1** (`_watch.scss`: 70ch max-width, line-height 1.65, secondary
  colour, link styling — verified live). **Done (Batch 4b):** `DescriptionParser` +
  `DescriptionText` replace `FormatAsHtml`; no `MarkupString` anywhere, so escaping is the
  framework's job and timestamps can be real buttons. Your markdown hunch checked out — 41 of
  508 descriptions in the library use `## ` headings (none use bold).
- `[x]` **[L] Subtitles in the player** with language selection, labelling
  autogenerated vs human. _We already download subs; need to expose the sidecar tracks to
  the video element as `<track>`s + a picker._ **Done (Batch 4b).** Auto-vs-human turned out to
  be recoverable after all: YouTube's ASR tracks carry per-word timing tags that hand-written
  ones never do, so it's sniffed from the file rather than needing a schema change.
- `[x]` **[S] Subscription name → link** to that subscription's page (from the player).
- `[x]` **[S] "Mark for deletion" button** in the player. _Ties into the existing scheduled-
  deletion flow (`ProcessScheduledDeletionsJob` / `Video.DownloadSkipped` family)._
- `[x]` **[M] "Up next" should list downloaded videos first**, and each should show its
  badges (same badge set as the video list).
- `[x]` **[M] Show download progress in the player.** If a video is downloading, show that
  in the watch view so the user can wait and watch the moment it lands. _Ties to the live
  progress feed + the per-video `download:{videoId}` notification key._

## C. Video list / badges

- `[x]` **[M] Badges are hard to see on busy thumbnails.** Add a distinct **"queued"** icon;
  while downloading show the **"downloaded"** icon under the progress pie; darken the
  thumbnail background a touch so overlay icons read clearly. _Done (Batch 1, uncommitted): new
  `Icons.DownloadQueued` (schedule/clock) for queued; downloading branch renders pie + downloaded
  glyph beneath (`video-badge-download`/`-under-pie`); top scrim on `.video-image-wrapper::after`.
  Verified live (queued + downloaded badges) + harness (download stack)._

## D. Downloads — correctness

- `[x]` **[M] "Download again" is broken — it must actually re-download.** Observed: clicking
  **Download again** failed with "video already downloaded" even though the files were
  **missing / incomplete** (a partial prior download). The explicit user action should
  always re-download, unconditionally — not short-circuit on `DownloadedPath != null`.
  _Root cause is the same early return the restart-resume no-op uses (`DownloadedPath != null`
  → return). Fix: a **forced** flag on the user-initiated job that bypasses that check (and
  cleans up any partial file first). The restart-reconciliation path stays on the no-op —
  the two must not share the same unconditional short-circuit._
- `[ ]` **[M] Reprocess downloaded videos** — e.g. fetch missing subtitles without a full
  re-download. _A lighter job variant that runs yt-dlp for subs/metadata only._
- `[ ]` **[L] Periodically refresh metadata** (view counts, likes/dislikes, title changes)
  for existing videos. _Recurring job; respects the throttle plan's pacing._

## E. Settings — structure & polish

- `[x]` **[S] Show what "Default / unset / inherit" resolves to.** Every inheritable setting
  should say the effective default inline, e.g. "Default (Newest first)". _Done (Batch 2, `0c4ae8b`):
  config DTOs now ship the resolved parent-scope value; `RgSimpleInputSelect.DefaultValueText`._
- `[x]` **[M] Fold Admin settings into the normal Settings page** as a section (gated by
  role), not a separate special button/page. _Done (Batch 2, `17f4aec`): admin-gated tab; `/admin`
  redirects; standalone nav icon removed._
- `[x]` **[M] Restyle the settings pages** — card backgrounds, better layout, title
  typography. Applies to all settings pages **including Edit Subscription**. _Done (Batch 2,
  `17f4aec` + `3f71b9b`): new `_settings.scss` cards + Advent Pro headings; edit pages carded and
  scrolling with the main pane._
- `[x]` **[M] Mirror subscription-only settings into user settings** ("Delete files when
  watched", etc.). _Done (Batch 2, `0c4ae8b`): seven inheritable options added to user settings via a
  "Subscription defaults" card; verified the sub-level inherit resolves to the user value._
- `[x]` **[M] Storage & quota bar** in settings — show a usage bar when limits are set. _Already
  existed end-to-end (`UserQuotaService` + `/api/settings/usage`); restyled in Batch 2._

## F. Settings — new options / behaviour changes

- `[x]` **[M] "Create subscription" spins for ~3 minutes — unacceptable, must return fast.**
  Observed: pressing **Create** blocked the UI for roughly 3 minutes. The create call must
  return near-instantly. Fix: fast-path validation only — validate/recognize the URL, create
  the sub row, and defer **all** metadata/enrichment/first-sync to background jobs; the UI
  returns as soon as the URL is accepted. _(Exempting from the host throttle alone isn't
  enough — the 3 min is the synchronous enrich/first-fetch on the request thread.)_
- `[x]` **[M] Per-user `cookies.txt`.** _The throttling plan currently has one global
  cookies file for all users; this promotes it to a per-user setting. Privacy + ban-blast
  radius both improve. Affects the Phase-A cookies design._
- `[x]` **[M] SponsorBlock: skip by default in the player.** Use SponsorBlock's own default
  categories to skip, not "keep everything". _Player-side skipping using stored segments._
- `[x]` **[L] Include Shorts / include member-only videos** — user default + per-subscription
  override, both **off** by default. Member videos need login cookies. **Done (Batch 5a).** Both are
  decided during sync, so an excluded video is never stored; turning an option back on picks the
  videos up on the next sync, since the whole channel is re-listed each run. Detection is by URL
  shape for Shorts and yt-dlp's `availability: subscriber_only` for member videos, both of which are
  present in a plain flat listing. Duration is deliberately **not** used — videos 187 and 193 in your
  own library are 32 s and 9 s and are ordinary uploads.
- `[x]` **[S] Default "Mark watched when files deleted" = false.** Change the shipped
  default. _Done (Batch 1, uncommitted): `Options.cs` `Subscriptions_MarkDeletedAsWatched`
  default `true`→`false`, no migration. Code+build verified; not exercised behaviourally (would
  mean deleting real downloads). Existing explicit overrides still win._
- `[x]` **[M] More subscription filters** beyond title — publish date `>=` / `<=`. **Done (Batch 5a)**
  as `PublishDateFilter` beside `SubscriptionFilterExtensions`, applied in the same in-memory half of
  the candidate query (the `DateTimeOffset` gotcha holds). Both ends inclusive; the window applies to
  automatic download only, never to a download you start yourself.
- `[x]` **[S/M] Set the icon of manual subscriptions.** Manual (non-provider) subs can't get
  an icon set; allow uploading/choosing one. _Done (uncommitted): `POST api/subscription/set_icon`
  (base64 JSON, raster allowlist rejecting SVG, 5 MB cap) + preview/upload on the edit page; works for
  every subscription, not just manual ones._

## G. Subscriptions

- `[x]` **[S/M] Subscription icon doesn't refresh automatically** — stays stale until a
  page reload. _Done (uncommitted). Root cause turned out to be systemic: the whole change-broadcast
  subsystem was disconnected. Replaced with an EF SaveChanges change feed, so icons (and every other
  entity change) push live. See the live-update work._

## H. Ops / maintenance / admin

- `[ ]` **[L] Server log page in Settings** — inspect server logs from the UI. _Read the
  `Logs/` files (or a ring buffer); admin-gated._
- `[ ]` **[M] Maintenance actions** — DB `VACUUM` and similar housekeeping, triggerable
  and/or scheduled.
- `[ ]` **[M] Periodic DB backups.** _Recurring job; timestamped copies (aligns with my
  global "back up before destructive ops" rule)._

---

## Proposed batches

Ordered by value-for-effort and dependency. Each batch is a small, single-focus plan.

**Batch 1 — Quick UX wins** (mostly frontend, low risk)
- A: bell dot colour, History/Clear-all alignment, no fake ETA on queued
- B: description typography
- C: video-list badges (queued icon, downloaded-under-pie, darken bg)
- E: "Default (…)" effective-value labels
- F: default "Mark watched when files deleted" = false

**Batch 2 — Settings pages overhaul**
- E: restyle settings + Edit Subscription (cards/layout/typography)
- E: fold Admin into Settings as a role-gated section
- E: mirror subscription settings into user settings
- E: storage & quota bar
- F/G: manual-sub icon + auto-refreshing sub icon

**Batch 3 — Download correctness & throttle-priority** (overlaps the throttling plan)
- D: "Download again" verifies the file exists (forced flag)
- F: UI create not throttled / fast-path validation
- F: per-user cookies
- A: richer notifications (speed/ETA/size), cancel queued/retrying

**Batch 4a — Player quick wins (2026-08-30, done)** — likes/dislikes, subscription link,
mark-for-deletion, download progress, up-next ordering. SponsorBlock skip-in-player turned out to be
**already implemented** (`RegardHelpers.addSkipSegmentsHandler`, wired from `Watch.razor`), so it was
struck from the batch rather than rebuilt.

**Batch 4b — the two that needed design (2026-08-30, done)** — subtitles in the player and the
description renderer. **Section B is now complete.** Notes worth keeping:
- Auto-generated vs human captions *is* recoverable from the files (per-word timing tags in YouTube's
  ASR output), so no schema change and it works retroactively on everything already downloaded.
- The CC control is **ours, overlaid on the player**, not the browser's: Chrome files its captions
  control inside the `⋮` overflow menu and offers no way to put it in the control bar. Both menus drive
  the same text tracks and stay in sync through the tracks' `change` event.
- Cues are centred by stripping per-cue `align`/`position` server-side (YouTube's ASR files pin them
  left) and styled with `::cue` — outline instead of the default opaque black box.
- Tracks are served over `/api/video/subtitle` with a query-string token, whitelisted alongside the
  video stream, with `crossorigin="anonymous"` on the player. Mounting every language costs nothing —
  a `disabled` track is never fetched.
- `video.js` is loaded but was never initialised only by luck; its `autoSetup` re-polls every 1 ms until
  window load. Removing `data-setup="{}"` from `Video.razor` makes that deterministic.
- No migration: `SubtitleTracks` is discovered from disk, so like `Chapters` it must stay out of
  `ToApi` / `VideoProjected` / `MergeLiveFields`.

**Batch 4 — Player features**
- B: subtitles in player (lang + autogen label)
- B: likes/dislikes (RYD bug — fix fetch→render)
- B: description linkify + seekable timestamps + light markdown + typography
- B: subscription name → link; "mark for deletion" button
- B: download progress in player
- B: Up-next downloaded-first + badges
- F: SponsorBlock skip-in-player

**Batch 5a — Content scope & filters (2026-08-30, done)** — include Shorts, include member-only
videos, publish-date window. No migration: the four options live in the existing key/value option
store, and the members-only signal rides a `[NotMapped]` property from the provider to the sync job.
Notes worth keeping:
- **A channel subscription's URL is normalised to its `/videos` tab** at creation
  (`YouTubeUrlHelper.FixYouTubeChannelUri`), and that tab excludes Shorts by construction — they live
  in their own tab. So "Include Shorts", when on, also lists the sibling `/shorts` tab; otherwise the
  option could never include anything. With it off nothing extra is fetched.
- **Members-only videos are already in your library.** 45 of the 194 CGP Grey videos report
  `availability: subscriber_only`; they were ingested before this existed and stay put. From now on
  they're skipped at sync, and the job log says so. Turning the option off does not remove what's
  already stored.
- The date window is **two-stage**. Un-enriched videos carry `Published = MinValue` as a sort
  placeholder, so the candidate query lets them through and `DownloadVideoJob` re-checks after
  enrichment, marking an out-of-window video `DownloadSkipped` rather than downloading it. Only
  automatic downloads are gated — an explicit click always wins, via a new `Auto` job-data flag.
- Bounds are `yyyy-MM-dd` strings, not `DateTimeOffset?`: that's what `<input type="date">` produces,
  what the option store's existing string overloads persist, and it makes `""` mean "no bound" free.

**Batch 5b — Reprocess & refresh**
- D: reprocess downloaded (missing subtitles)
- D: periodic metadata refresh

**Batch 6 — Ops & maintenance**
- H: server log page
- H: maintenance actions (VACUUM etc.)
- H: periodic DB backups

**Batch 3 status (2026-08-30): all four items implemented, uncommitted.** Measured results: create
returns in ~0.05 s (was ~3 min) and the Add dialog closes in 0.3 s; "Download again" sweeps the old
files (both the recorded path and the freshly-resolved one, which is what catches a half-finished
download) and re-fetches; download cards carry speed/ETA/size; queued and retrying downloads can be
cancelled. Three pre-existing bugs were found on the way and filed in `BACKLOG.md` — the idle watchdog
killing large healthy downloads (a likely root cause of the original report), `RetryCount` never
decrementing, and no JS runtime for yt-dlp.

_Batch 3 deliberately sits next to the existing throttling plan
(`~/.claude/plans/indexed-discovering-penguin.md`) — per-user cookies changes its Phase-A
cookies design, and "UI create not throttled" is the user-facing complement to its
reschedule-based throttle. Worth reconciling the two before starting Batch 3._
