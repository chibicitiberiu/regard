@* Minimal layout for the first-run setup wizard: no app chrome (nav/subscription pane),
   just the centered wizard card on the app background. Used regardless of auth state so a
   partially-authenticated setup doesn't render the full app shell. *@
@inherits LayoutComponentBase

@Body
