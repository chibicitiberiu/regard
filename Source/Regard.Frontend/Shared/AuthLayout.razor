@* Minimal, auth-agnostic layout for the standalone account pages (login / register / forgot /
   reset password). MainLayout wraps everything in an AuthorizeView and swaps the routed page body
   for the login modal when unauthenticated, so a logged-out user could never reach these pages
   under it. Each page renders its own SetupCard (the same card the wizard uses), so this layout
   just passes the body through on the app background. Mirrors SetupLayout. *@
@inherits LayoutComponentBase

@Body
